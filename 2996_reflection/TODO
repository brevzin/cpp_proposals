* [ ] Split up `^e` is only ever a reflection of that entity, `^(e)` is a reflection of an expression
      The interesting case here is `^p.first` - can I reflect that entity?
* [ ] `X<^std::string>` needs to have external linkage
* [ ] is_noexcept. What to do about conditionally explicit?
* [ ] is_{l,r,}reference_qualified()
